#include <stdio.h>

int main()
{
int a = 8, b = 6, c = 3, d = 4, e = 5, f = 1;
int Z;

//Z = a*b / c*d; // Valor = 64
//Z = (a*b) / c*d; // Valor = 64
//Z = a*b / (c*d); // Valor = 4
//Z = (a*b / c*d); // Valor = 64
//Z = (a*b) / (c*d); // Valor = 4
//Z = f + d * e + a / d + d; // Valor = 27
//Z = f + d * e - a / d + d; // Valor = 23

printf("Valor de Z es %d", Z);





}
